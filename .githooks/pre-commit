#!/bin/sh
# Refuse commits on main: work lands via PRs from branches.
# Deliberate override: git commit --no-verify
if [ "$(git branch --show-current)" = "main" ]; then
  echo "pre-commit: refusing to commit on main; branch first" >&2
  exit 1
fi
